const BASE=self.location.pathname.replace(/\/sw\.js$/,'/');const CACHE='south-movies-shell-v2';const ASSETS=[BASE,BASE+'assets/styles.css',BASE+'assets/app.js',BASE+'manifest.json',BASE+'assets/icon.svg'];self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)).then(()=>self.skipWaiting())));self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==location.origin)return;if(u.pathname.includes('/data/')||u.hostname==='www.youtube.com'||u.hostname==='youtube.com')return;e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request).then(x=>{if(e.request.method==='GET'){const c=x.clone();caches.open(CACHE).then(k=>k.put(e.request,c))}return x}).catch(()=>caches.match(BASE))))});
